<snippet>
  <content><![CDATA[
create table ${1:gnu} as
select  ${2:*}
from    $3 as $4
${5:where $4.something in ('bibbity', 'bobbity', 'boo')}
${6:order by  $2}
;
]]></content>
  <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
  <tabTrigger>ct</tabTrigger>
  <!-- Optional: Set a scope to limit where the snippet will trigger -->
  <scope>source.sas</scope>
  <description>create table (NO join)</description>
</snippet>
